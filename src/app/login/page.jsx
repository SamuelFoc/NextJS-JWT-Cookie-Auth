import { Form } from "../../components/Form/Form";
import styles from "./page.module.scss";

const page = () => {
  return (
    <main className={styles.main}>
      <Form />
    </main>
  );
};

export default page;
